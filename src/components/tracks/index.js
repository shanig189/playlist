import React from 'react';
import Search from '../search';
import Filter from '../filter';

const Tracks = () => {
    return(
        <div>
            <Search />
            <Filter />
        </div>
    )
}

export default Tracks;