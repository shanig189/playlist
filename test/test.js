const assert = require('assert');

describe('Track list test', function() {
    it('should add track to list and return list size 1', function() {
        //make request to the trackList api
        assert(1 === 1);
        //add one more
        //assert check that size is 2 and the track as been added
    });
});